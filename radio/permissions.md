# Permissions

## capabilities
- commands
- filesystem

## secrets
(none)

## allowed_commands
- ngrok — expose the radio stream publicly
- ffprobe — detect audio file metadata
- which — check if ngrok is installed
